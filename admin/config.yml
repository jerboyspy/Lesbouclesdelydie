backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

# Désactive l'aperçu à droite pour gagner de la place sur iPhone
editor:
  preview: false

collections:
  - name: "produits"
    label: "💎 Mes Bijoux"
    folder: "content/produits"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Nom du Produit", name: "title", widget: "string"}
      
      - {label: "Prix (€)", name: "price", widget: "number", value_type: "float", min: 0, step: 0.01}
      
      - {label: "Quantité en Stock", name: "stock", widget: "number", default: 1, min: 0}
      
      - {label: "Afficher sur le site ?", name: "available", widget: "boolean", default: true}
      
      - label: "Badge spécial"
        name: "badge"
        widget: "select"
        options: ["Aucun", "Nouveau", "Promo", "Coup de cœur"]
        default: "Aucun"
        
      - {label: "Image du Produit", name: "image", widget: "image"}
      
      - {label: "Description (matériaux, taille...)", name: "body", widget: "markdown", required: false}
